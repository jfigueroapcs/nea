---
// import Welcome from '../components/Welcome.astro';
import Layout from '../layouts/Layout.astro';
// import {
// 	getSecret
// } from "astro:env/server"

// console.log("==>", getSecret('SmartPort-Key'))

import SmartPort from '../lib/SmartPort';

const cache = new SmartPort({
	endpoints: ['event', 'events', 'tourney', 'juegos', 'users?delegation=true']
})

cache.refresh('events').then(() => {
	console.log('Cache actualizado');
})


---

<Layout title="Nacionales Deportivos" header={1}>
</Layout>
